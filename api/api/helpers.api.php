<?php

// GET to /settings to get helper settings (helpers only).

// POST to /settings to update helper settings (helpers only).

// POST to /upgrade to upgrade to a helper account (non-helpers only).

// POST to /upgrade/trust to verify another helper's request to be a helper (helpers only).

// POST to /upgrade/verify to verify identity through social media (non-helpers only).